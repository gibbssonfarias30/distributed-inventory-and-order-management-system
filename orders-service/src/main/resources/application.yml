server:
  port: 8988

spring:
  application:
    name: orders-service
  datasource:
    url: jdbc:mysql://localhost:3306/ms_orders?serverTimezone=UTC
    username: root
    password: gibb
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update
